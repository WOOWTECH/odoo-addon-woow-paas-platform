---
name: Marketplace UI
status: open
created: 2026-02-01T17:23:19Z
updated: 2026-02-01T17:39:37Z
github: https://github.com/WOOWTECH/odoo-addon-woow-paas-platform/issues/8
depends_on: [7]
parallel: true
conflicts_with: [9, 10, 11, 12]
---

# Task: Marketplace UI

## Description

建立 Application Marketplace 頁面，讓用戶可以瀏覽、搜尋和篩選可部署的應用程式。

## Acceptance Criteria

- [ ] AppMarketplacePage 頁面建立
- [ ] AppCard 元件建立（顯示應用資訊）
- [ ] CategoryFilter 元件建立
- [ ] 搜尋功能實現
- [ ] 分類篩選功能實現
- [ ] 點擊應用可導航到配置頁面
- [ ] 載入狀態和空狀態處理

## Technical Details

### Router 更新 (`src/static/src/paas/core/router.js`)

```javascript
// 新增路由
'#/workspaces/:id/services/marketplace': 'marketplace',
```

### AppMarketplacePage (`src/static/src/paas/pages/marketplace/`)

```
pages/marketplace/
├── AppMarketplacePage.js
├── AppMarketplacePage.xml
└── AppMarketplacePage.scss
```

**Features:**
- 搜尋欄位（即時搜尋）
- 分類 tabs（All, AI & LLM, Automation, Database, etc.）
- 應用卡片 grid layout
- 點擊「+」按鈕導航到配置頁面

### AppCard (`src/static/src/paas/components/marketplace/`)

```
components/marketplace/
├── AppCard.js
├── AppCard.xml
└── AppCard.scss
```

**顯示內容:**
- Icon
- Name
- Short description
- Tags (badges)
- Monthly price
- 「+」按鈕

### CategoryFilter (`src/static/src/paas/components/marketplace/`)

```
components/marketplace/
├── CategoryFilter.js
├── CategoryFilter.xml
└── CategoryFilter.scss
```

**Categories:**
- All
- AI & LLM
- Automation
- Database
- Analytics
- DevOps

### cloud_service.js (`src/static/src/paas/services/`)

```javascript
export const cloudService = {
    async getTemplates(category, search) {
        return await jsonrpc('/woow/api/cloud/templates', { category, search });
    },
    // ...
};
```

### SCSS Styles

複用現有 SCSS 變數和 patterns：
- `.o_woow_marketplace_page`
- `.o_woow_app_card`
- `.o_woow_category_filter`

## Dependencies

- [ ] Task 003: API 可用

## Effort Estimate

- Size: M
- Hours: 8-12
- Parallel: true (can run with 005-008)

## Definition of Done

- [ ] 頁面可正常載入並顯示應用列表
- [ ] 搜尋功能正常運作
- [ ] 分類篩選功能正常運作
- [ ] 響應式設計（desktop + tablet）
- [ ] 載入狀態顯示 spinner
- [ ] 空狀態顯示適當訊息
