---
name: Dashboard Page
status: closed
created: 2026-01-13T17:39:27Z
updated: 2026-01-14T06:51:32Z
github: https://github.com/WOOWTECH/odoo-addons/issues/28
depends_on: [25, 27]
parallel: true
conflicts_with: [29, 30]
---

# Task: Dashboard Page

## Description

實作 Dashboard 頁面元件，展示歡迎訊息、統計卡片、最近活動、快速存取區塊。資料暫時使用靜態 mock。

## Acceptance Criteria

- [ ] 建立 `pages/dashboard/DashboardPage.js` 和 `.xml`
- [ ] 建立 `styles/pages/dashboard.scss` 頁面樣式
- [ ] 顯示歡迎訊息（Welcome back, Sarah!）
- [ ] 顯示 3 個統計卡片（Active Instances, Secure Tunnels, System Health）
- [ ] 顯示 Recent Activity 區塊（時間軸樣式）
- [ ] 顯示 Quick Access 區塊（4 個服務卡片）
- [ ] 響應式：Mobile 時統計卡片堆疊

## Technical Details

### 檔案結構
```
static/src/paas/pages/dashboard/
├── DashboardPage.js
└── DashboardPage.xml
```

### Mock 資料
```javascript
const stats = [
    { label: "Active Instances", value: "8", icon: "dns", note: "All systems running optimally." },
    { label: "Secure Tunnels", value: "3", icon: "encrypted", note: "Connected to on-premise..." },
    { label: "System Health", value: "Healthy", icon: "ecg_heart", color: "green", note: "" },
];

const activities = [
    { time: "10:48 AM", user: "Admin", action: "deployed", target: "Odoo Cloud" },
    { time: "10:41 AM", user: "Admin", action: "deployed", target: "Odoo Cloud" },
    { time: "10:27 AM", user: "Admin", action: "deployed", target: "Home Assistant" },
    { time: "10:21 AM", user: "Admin", action: "deployed", target: "Odoo Cloud" },
    { time: "10:01 AM", user: "Admin", action: "deployed", target: "Odoo Cloud" },
];

const quickAccess = [
    { name: "Odoo Cloud", icon: "cloud_circle", color: "purple", action: "Deploy" },
    { name: "Local Home Assistant", icon: "home_iot_device", color: "blue", tag: "Local", action: "Connect" },
    { name: "n8n Cloud", icon: "webhook", color: "orange", action: "Deploy" },
    { name: "Custom Local Server", icon: "dns", color: "slate", tag: "On-Prem", action: "Setup Tunnel" },
];
```

### 佈局結構
```
┌─────────────────────────────────┐
│ Welcome back, Sarah!            │
│ Here is what is happening...    │
├─────────┬─────────┬─────────────┤
│ Stat 1  │ Stat 2  │ Stat 3      │
├─────────┴─────────┼─────────────┤
│ Recent Activity   │ Quick Access│
│ (Timeline)        │ (4 cards)   │
└───────────────────┴─────────────┘
```

### 樣式要點
- 統計卡片：3 欄 grid，mobile 時 1 欄
- Activity Timeline：左側時間，中間頭像，右側文字
- Quick Access：2x2 grid

### 參考原型
- `resource/stitch_paas_web_app_shell_global_navigation/paas_web_app_shell_-_global_navigation_1/code.html`

## Dependencies

- [ ] Task 003 完成（AppShell 佈局）
- [ ] Task 005 完成（UI 元件）

## Effort Estimate

- Size: M
- Hours: 2-3
- Parallel: true (可與 Task 007, 008 並行但有檔案衝突)

## Definition of Done

- [ ] Dashboard 頁面正確渲染
- [ ] 統計卡片使用 WoowCard 元件
- [ ] 按鈕使用 WoowButton 元件
- [ ] 圖示使用 WoowIcon 元件
- [ ] 響應式佈局正常
- [ ] 樣式符合原型設計
