---
name: Empty State Page
status: closed
created: 2026-01-13T17:39:27Z
updated: 2026-01-14T06:51:32Z
github: https://github.com/WOOWTECH/odoo-addons/issues/30
depends_on: [25, 27]
parallel: true
conflicts_with: [28, 29]
---

# Task: Empty State Page

## Description

建立通用的 Empty State 佔位頁面元件，用於尚未實作的頁面（Deployments, Billing, Settings）。顯示 "Coming Soon" 或類似訊息。

## Acceptance Criteria

- [ ] 建立 `pages/empty/EmptyState.js` 和 `.xml`
- [ ] 建立 `styles/pages/empty.scss` 頁面樣式
- [ ] 顯示頁面名稱（從 router 取得）
- [ ] 顯示 "Coming Soon" 圖示和訊息
- [ ] 顯示簡短描述說明此功能即將推出
- [ ] 視覺居中且美觀

## Technical Details

### 檔案結構
```
static/src/paas/pages/empty/
├── EmptyState.js
└── EmptyState.xml
```

### 元件設計
```javascript
export class EmptyState extends Component {
    static template = "woow_paas_platform.EmptyState";
    static props = {
        pageName: { type: String, optional: true },
    };

    setup() {
        this.router = useState(router);
    }

    get displayName() {
        return this.props.pageName || this.getNameFromRoute();
    }

    getNameFromRoute() {
        const route = router.routes.find(r => r.path === router.current);
        return route ? route.name : "Page";
    }
}
```

### 佈局結構
```
┌─────────────────────────────────┐
│                                 │
│         🚀                      │
│    [Page Name]                  │
│    Coming Soon                  │
│                                 │
│  This feature is under         │
│  development and will be       │
│  available shortly.            │
│                                 │
└─────────────────────────────────┘
```

### 樣式要點
- 垂直水平居中
- 圖示使用大尺寸 Material Symbol（如 rocket_launch）
- 淡色文字，輕量視覺

## Dependencies

- [ ] Task 003 完成（AppShell 佈局）
- [ ] Task 005 完成（UI 元件）

## Effort Estimate

- Size: XS
- Hours: 0.5-1
- Parallel: true (可與 Task 006, 007 並行但有檔案衝突)

## Definition of Done

- [ ] Empty State 頁面正確渲染
- [ ] 顯示正確的頁面名稱
- [ ] 視覺美觀且居中
- [ ] 適用於 Deployments, Billing, Settings 三個路由
