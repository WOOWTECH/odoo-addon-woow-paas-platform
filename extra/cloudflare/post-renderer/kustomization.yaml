apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Helm output is piped in as stdin resources
resources:
  - all.yaml

patches:
  - path: patch-deployment.yaml
    target:
      kind: Deployment
      name: ".*cloudflare-tunnel.*"
