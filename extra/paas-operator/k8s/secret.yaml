---
apiVersion: v1
kind: Secret
metadata:
  name: paas-operator-secret
  namespace: default
  labels:
    app: paas-operator
type: Opaque
stringData:
  # IMPORTANT: Change this to a secure random value before deployment!
  # Generate with: openssl rand -base64 32
  api-key: "CHANGEME_GENERATE_SECURE_KEY"
