<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="login_layout" inherit_id="web.login_layout">
        <xpath expr="//div[hasclass('card-body')]" position="replace">
            <t t-set="disable_footer" t-value="request.env['ir.config_parameter'].sudo().get_param('mommy.database_manager')"/>
            <div class="card-body">
                <div class="text-center pb-3 border-bottom mb-4">
                    <img t-attf-src="/web/binary/company_logo{{ '?dbname='+db if db else '' }}" alt="Logo" style="max-height:120px; max-width: 100%; width:auto"/>
                </div>
                <t t-out="0"/>
                <div class="text-center small mt-4 pt-3 border-top" t-if="not disable_footer">
                    <t t-if="not disable_database_manager">
                        <a class="border-end pe-2 me-1" href="/web/database/manager">Manage Databases</a>
                    </t>
                    <a href="https://www.odoohub.com.cn" target="_blank">Powered by <span>OHM</span></a>
                </div>
            </div>
        </xpath>
    </template>

    <template id="brand_promotion_message" name="Brand Promotion Message">
        <t t-set="odoo_logo">
            <a target="_blank"
                t-attf-href="https://www.odoohub.com.cn"
                class="badge text-bg-light">
            </a>
        </t>
        <t t-set="final_message">Powered by %s%s</t>
        <t t-out="final_message % (odoo_logo, _message and ('- ' + _message) or '')"/>
    </template>
</odoo>